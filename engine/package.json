{
  "name": "@bazilionaire/engine",
  "version": "0.1.0",
  "description": "Deterministic Bazi (八字) computation engine — TypeScript port, verified against a lunar_python oracle in CI.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
