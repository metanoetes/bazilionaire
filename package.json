{
  "name": "bazilionaire",
  "private": true,
  "workspaces": ["engine", "app"],
  "scripts": {
    "test": "bun run --cwd engine test",
    "typecheck": "bun run --cwd engine typecheck",
    "check:nayin": "bun run --cwd app check:nayin",
    "check:reading": "bun run --cwd app check:reading",
    "check:tutor": "bun run --cwd app check:tutor",
    "check:verify": "bun run --cwd app check:verify",
    "check:commons": "bun run --cwd app check:commons",
    "dev": "bun run --cwd app dev",
    "build": "bun run --cwd app build"
  }
}
